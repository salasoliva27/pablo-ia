---
phase: 02-shell-layout
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - dashboard/frontend/src/hooks/useKeyboardShortcuts.ts
  - dashboard/frontend/src/hooks/useWebSocket.ts
  - dashboard/frontend/src/components/ShellLayout.tsx
  - dashboard/frontend/src/components/BottomPanelSwitcher.tsx
  - dashboard/frontend/src/App.tsx
  - dashboard/bridge/server.ts
autonomous: true
requirements:
  - LAYOUT-04
  - LAYOUT-05

must_haves:
  truths:
    - "Cmd/Ctrl+B toggles the chat panel collapsed/expanded"
    - "Cmd/Ctrl+J toggles the bottom panel collapsed/expanded"
    - "Cmd/Ctrl+\\ toggles the workspace panel collapsed/expanded"
    - "Bottom panel shows tab buttons for Brain Zoom, Project Detail, Tool Registry"
    - "App connects to bridge WebSocket and shows connection status"
  artifacts:
    - path: "dashboard/frontend/src/hooks/useKeyboardShortcuts.ts"
      provides: "Global keyboard shortcut handler for panel toggles"
      exports: ["useKeyboardShortcuts"]
    - path: "dashboard/frontend/src/hooks/useWebSocket.ts"
      provides: "Bridge WebSocket connection hook"
      exports: ["useWebSocket"]
    - path: "dashboard/frontend/src/components/BottomPanelSwitcher.tsx"
      provides: "Tab switcher for pull-up bottom panel content"
      exports: ["BottomPanelSwitcher"]
  key_links:
    - from: "useKeyboardShortcuts"
      to: "ImperativePanelHandle refs"
      via: "collapse/expand calls"
      pattern: "panelRef\\.current\\?\\.(collapse|expand)"
    - from: "useWebSocket"
      to: "ws://localhost:3100"
      via: "WebSocket connection"
      pattern: "new WebSocket"
---

<objective>
Add keyboard shortcuts for panel collapse/expand, pull-up bottom panel with tab switcher, and WebSocket connection to the bridge server.

Purpose: Complete the shell interactivity -- panels toggle with keyboard, bottom panel slides up with content tabs, and the frontend is wired to the bridge.
Output: Fully interactive shell layout with keyboard shortcuts and live bridge connection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shell-layout/02-CONTEXT.md
@.planning/phases/02-shell-layout/02-01-SUMMARY.md

<interfaces>
From Plan 01 (ShellLayout.tsx):
- ShellLayout exposes ImperativePanelHandle refs for chatPanel, bottomPanel, workspacePanel
- Uses react-resizable-panels with autoSaveId

From dashboard/bridge/types.ts:
```typescript
export type ClientMessage =
  | { type: "start"; prompt: string; cwd?: string }
  | { type: "follow_up"; prompt: string }
  | { type: "permission_response"; id: string; allowed: boolean }
  | { type: "interrupt" };

export type ServerMessage =
  | { type: "claude_message"; message: unknown }
  | { type: "permission_request"; id: string; toolName: string; input: Record<string, unknown> }
  | { type: "tool_event"; toolName: string; input: unknown; sessionId: string; timestamp: number }
  | { type: "fs_event"; event: string; path: string; timestamp: number }
  | { type: "error"; message: string }
  | { type: "session_end"; cost?: number; usage?: unknown };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keyboard shortcuts + WebSocket hook + bottom panel tabs</name>
  <files>
    dashboard/frontend/src/hooks/useKeyboardShortcuts.ts,
    dashboard/frontend/src/hooks/useWebSocket.ts,
    dashboard/frontend/src/components/BottomPanelSwitcher.tsx,
    dashboard/frontend/src/components/ShellLayout.tsx,
    dashboard/frontend/src/App.tsx
  </files>
  <action>
    1. Create `dashboard/frontend/src/hooks/useKeyboardShortcuts.ts` (per D-05):
       - Accept refs: `{ chatPanel: ImperativePanelHandle | null, bottomPanel: ImperativePanelHandle | null, workspacePanel: ImperativePanelHandle | null }`
       - useEffect with keydown listener:
         - Cmd/Ctrl+B: toggle chatPanel (isCollapsed ? expand() : collapse())
         - Cmd/Ctrl+J: toggle bottomPanel
         - Cmd/Ctrl+\: toggle workspacePanel
       - All shortcuts call e.preventDefault()
       - Cleanup on unmount

    2. Create `dashboard/frontend/src/hooks/useWebSocket.ts` (per D-06):
       - Connects to `ws://${window.location.hostname}:3100` (or via Vite proxy at `/ws`)
       - Returns: `{ status: 'connecting' | 'connected' | 'disconnected', lastMessage: ServerMessage | null, send: (msg: ClientMessage) => void }`
       - Auto-reconnect with exponential backoff (1s, 2s, 4s, max 10s)
       - Parses incoming JSON as ServerMessage
       - Uses useRef for WebSocket instance, useCallback for send

    3. Create `dashboard/frontend/src/components/BottomPanelSwitcher.tsx` (per D-04, LAYOUT-04):
       - Props: none (content is placeholder for now)
       - State: activeTab: 'brain-zoom' | 'project-detail' | 'tool-registry'
       - Renders a tab bar at top with three buttons styled with CSS vars
       - Active tab has accent color underline
       - Below tabs: PanelPlaceholder showing the active tab name
       - Tab bar height ~32px, compact design

    4. Update `ShellLayout.tsx`:
       - Create refs with useRef<ImperativePanelHandle>(null) for chat, bottom, workspace panels
       - Assign refs to the collapsible ResizablePanel components
       - Replace bottom panel PanelPlaceholder with BottomPanelSwitcher
       - Export/expose refs (either via context or by calling useKeyboardShortcuts inside ShellLayout)
       - Call useKeyboardShortcuts with the three refs

    5. Update `App.tsx`:
       - Call useWebSocket()
       - Show connection status indicator: small dot in top-right corner
         - Green dot + "Connected" for connected
         - Yellow dot + "Connecting..." for connecting
         - Red dot + "Disconnected" for disconnected
       - Pass ws send/status as context or props if needed (for Phase 3)
  </action>
  <verify>
    <automated>cd /workspaces/venture-os/dashboard/frontend && npx tsc --noEmit && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>
    - Cmd/Ctrl+B collapses/expands chat panel
    - Cmd/Ctrl+J collapses/expands bottom panel
    - Cmd/Ctrl+\ collapses/expands workspace panel
    - Bottom panel shows three tab buttons (Brain Zoom, Project Detail, Tool Registry)
    - Connection status indicator shows in top-right
    - WebSocket connects to bridge with auto-reconnect
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire bridge to serve frontend static files in production mode</name>
  <files>
    dashboard/bridge/server.ts,
    dashboard/frontend/vite.config.ts,
    dashboard/package.json
  </files>
  <action>
    1. Update `dashboard/bridge/server.ts`:
       - After all API routes, add a static file middleware:
         ```
         const frontendDist = path.join(__dirname, '../frontend/dist');
         if (fs.existsSync(frontendDist)) {
           app.use(express.static(frontendDist));
           app.get('*', (req, res) => {
             res.sendFile(path.join(frontendDist, 'index.html'));
           });
         }
         ```
       - Import path and fs at the top
       - This means `venture-os` serves the built frontend when available, no separate Vite server needed in production
       - The catch-all `*` route must come AFTER /health and /hooks routes

    2. Update `dashboard/frontend/vite.config.ts`:
       - Set `build.outDir` to `dist` (default, but explicit)
       - Ensure the WebSocket proxy in dev mode targets the correct path

    3. Update `dashboard/package.json` scripts:
       - Add `"build:frontend": "cd frontend && npm run build"`
       - Add `"build": "npm run build:frontend && tsc"` (builds frontend then bridge)
       - Keep `"dev:all"` for development with both servers
  </action>
  <verify>
    <automated>cd /workspaces/venture-os/dashboard/frontend && npx vite build && cd /workspaces/venture-os/dashboard && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `npm run build:frontend` produces dashboard/frontend/dist/
    - Bridge server serves frontend/dist/ as static files when the directory exists
    - In dev mode, Vite dev server proxies to bridge; in production, bridge serves everything
    - `venture-os` command starts bridge which serves the full app on :3100
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client -> bridge | Untrusted messages from browser to bridge |
| Static file serving | Bridge serves frontend dist files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | T (Tampering) | WebSocket messages | mitigate | Bridge already validates with isValidClientMessage (from Phase 1) |
| T-02-04 | I (Info Disclosure) | Static file serving | accept | Localhost only, sole user, no sensitive data in frontend bundle |
| T-02-05 | D (Denial of Service) | WebSocket reconnect | mitigate | Exponential backoff with max 10s cap prevents reconnect storm |
</threat_model>

<verification>
1. `npm run dev:all` in dashboard/ starts both bridge (:3100) and Vite (:5180)
2. Opening :5180 shows three panels with connection status
3. Keyboard shortcuts toggle panels
4. Bottom panel shows tab switcher
5. `npm run build:frontend && npm run dev` serves the app from bridge on :3100
</verification>

<success_criteria>
- All 5 LAYOUT requirements fully addressed
- Keyboard shortcuts work for all three collapsible panels
- Bottom panel has tab switcher for pull-up content
- WebSocket connects to bridge with reconnect
- Bridge serves built frontend as static files
- Single `venture-os` command serves everything after build
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-layout/02-02-SUMMARY.md`
</output>
