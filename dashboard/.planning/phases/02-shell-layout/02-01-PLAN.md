---
phase: 02-shell-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/frontend/package.json
  - dashboard/frontend/tsconfig.json
  - dashboard/frontend/vite.config.ts
  - dashboard/frontend/index.html
  - dashboard/frontend/src/main.tsx
  - dashboard/frontend/src/App.tsx
  - dashboard/frontend/src/styles/tokens.css
  - dashboard/frontend/src/styles/global.css
  - dashboard/frontend/src/components/ShellLayout.tsx
  - dashboard/frontend/src/components/PanelPlaceholder.tsx
  - dashboard/package.json
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02
  - LAYOUT-03

must_haves:
  truths:
    - "Browser shows three horizontal panels (chat, graph, workspace) on page load"
    - "Dragging borders between panels resizes them"
    - "Panel sizes persist across browser refreshes via autoSaveId"
    - "All visual styles use CSS custom properties"
  artifacts:
    - path: "dashboard/frontend/src/components/ShellLayout.tsx"
      provides: "Three-panel resizable layout with react-resizable-panels"
      exports: ["ShellLayout"]
    - path: "dashboard/frontend/src/styles/tokens.css"
      provides: "CSS custom property definitions for all visual tokens"
      contains: "--color-bg-primary"
    - path: "dashboard/frontend/vite.config.ts"
      provides: "Vite config with proxy to bridge server on :3100"
  key_links:
    - from: "dashboard/frontend/src/App.tsx"
      to: "ShellLayout"
      via: "import and render"
      pattern: "import.*ShellLayout"
    - from: "dashboard/frontend/vite.config.ts"
      to: "bridge server"
      via: "proxy /api and /ws to localhost:3100"
      pattern: "proxy.*3100"
---

<objective>
Scaffold the Vite React frontend and build the three-panel resizable shell layout with CSS custom properties and localStorage persistence.

Purpose: This is the visual foundation -- after this plan, `npm run dev` in the frontend opens a browser with three draggable, persistent panels.
Output: Working React app with ShellLayout using react-resizable-panels, CSS tokens, Vite proxy to bridge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shell-layout/02-CONTEXT.md
@.planning/phases/01-foundation-bridge-server/01-01-SUMMARY.md

<interfaces>
From dashboard/bridge/types.ts:
```typescript
export type ServerMessage =
  | { type: "claude_message"; message: unknown }
  | { type: "permission_request"; id: string; toolName: string; input: Record<string, unknown> }
  | { type: "tool_event"; toolName: string; input: unknown; sessionId: string; timestamp: number }
  | { type: "fs_event"; event: string; path: string; timestamp: number }
  | { type: "error"; message: string }
  | { type: "session_end"; cost?: number; usage?: unknown };
```

From dashboard/bin/venture-os.ts:
- Bridge runs on PORT (default 3100)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React project with CSS tokens</name>
  <files>
    dashboard/frontend/package.json,
    dashboard/frontend/tsconfig.json,
    dashboard/frontend/vite.config.ts,
    dashboard/frontend/index.html,
    dashboard/frontend/src/main.tsx,
    dashboard/frontend/src/styles/tokens.css,
    dashboard/frontend/src/styles/global.css,
    dashboard/package.json
  </files>
  <action>
    1. Create `dashboard/frontend/` directory structure.

    2. Create `dashboard/frontend/package.json` with:
       - react, react-dom (^18.3)
       - react-resizable-panels (^4.10)
       - devDependencies: @types/react, @types/react-dom, @vitejs/plugin-react, vite, typescript

    3. Create `dashboard/frontend/tsconfig.json` with strict mode, jsx: react-jsx, paths alias "@/*" -> "src/*".

    4. Create `dashboard/frontend/vite.config.ts`:
       - React plugin
       - Proxy `/api` to `http://localhost:3100`
       - Proxy `/ws` to `ws://localhost:3100` (WebSocket upgrade)
       - Dev server on port 5180 (avoid conflicts with other projects on 5173/5174)
       - Resolve alias "@" -> src/

    5. Create `dashboard/frontend/index.html`:
       - Dark background inline style on body to prevent white flash
       - Blocking script that reads `localStorage.getItem('theme-preset')` and sets `data-theme` attribute
       - Script tag for `/src/main.tsx`
       - Root div with id="root" taking full viewport

    6. Create `dashboard/frontend/src/styles/tokens.css` (per D-03):
       - Define all CSS custom properties on `:root` and `[data-theme="dark"]`
       - Colors in OKLCH: bg-primary (dark ~0.13), bg-secondary (~0.17), bg-surface (~0.20), text-primary (~0.95), text-secondary (~0.70), text-muted (~0.50), accent (cyan ~0.75 0.18 180), danger, success
       - Border colors, radii, spacing scale
       - Animation speed/intensity multiplier vars (for Phase 8)
       - Font families (Inter for body/heading, JetBrains Mono for mono)

    7. Create `dashboard/frontend/src/styles/global.css`:
       - Import tokens.css
       - Reset: box-sizing border-box, margin 0, full viewport html/body/#root
       - body uses var(--color-bg-primary), var(--color-text-primary), var(--font-family-body)
       - Scrollbar styling for dark theme

    8. Create `dashboard/frontend/src/main.tsx`:
       - Import global.css
       - Render App into #root with StrictMode

    9. Update root `dashboard/package.json` scripts to add:
       - `"dev:frontend": "cd frontend && npm run dev"`
       - `"dev:all": "npm run dev & npm run dev:frontend"` (runs bridge + frontend)

    10. Run `cd dashboard/frontend && npm install`
  </action>
  <verify>
    <automated>cd /workspaces/venture-os/dashboard/frontend && npx tsc --noEmit && npx vite build --mode development 2>&1 | tail -5</automated>
  </verify>
  <done>Vite React project compiles, tokens.css defines all CSS custom properties, index.html has blocking theme script, proxy configured to bridge on :3100</done>
</task>

<task type="auto">
  <name>Task 2: Build ShellLayout with three resizable panels</name>
  <files>
    dashboard/frontend/src/App.tsx,
    dashboard/frontend/src/components/ShellLayout.tsx,
    dashboard/frontend/src/components/PanelPlaceholder.tsx
  </files>
  <action>
    1. Create `dashboard/frontend/src/components/PanelPlaceholder.tsx`:
       - Props: `{ name: string; icon?: string }`
       - Renders centered label with panel name, subtle border using CSS vars
       - Uses var(--color-bg-secondary) background, var(--color-text-muted) text
       - Full height/width, flexbox center

    2. Create `dashboard/frontend/src/components/ShellLayout.tsx` (per D-01, D-04):
       - Import ResizablePanelGroup, ResizablePanel, ResizableHandle from react-resizable-panels
       - Import "react-resizable-panels/dist/styles.css" for default handle styles
       - Outer horizontal group with `autoSaveId="venture-os-main"` (LAYOUT-02, LAYOUT-03):
         - Left panel (Chat): defaultSize={25}, minSize={15}, collapsible, collapsedSize={0}, ref={chatPanelRef}
         - ResizableHandle with `className` styled as 4px wide, var(--border-color) background, hover highlight
         - Center panel: defaultSize={45}, minSize={20} -- contains a NESTED vertical group with `autoSaveId="venture-os-center"`:
           - Top: defaultSize={100}, minSize={30} -- PanelPlaceholder "System Graph"
           - ResizableHandle (horizontal)
           - Bottom: defaultSize={0}, minSize={15}, collapsible, collapsedSize={0}, ref={bottomPanelRef} -- PanelPlaceholder "Bottom Panel"
         - ResizableHandle
         - Right panel (Workspace): defaultSize={30}, minSize={15}, collapsible, collapsedSize={0}, ref={workspacePanelRef}
       - Export refs for chat, bottom, workspace panels as `ImperativePanelHandle` via forwardRef or context
       - All panels use PanelPlaceholder with appropriate labels

    3. Create `dashboard/frontend/src/App.tsx`:
       - Import ShellLayout
       - Render ShellLayout as full-viewport component
       - Simple functional component, no state yet (WebSocket comes in Plan 02)

    4. Style the resize handles:
       - In ShellLayout or a CSS module: handles are 4px wide (vertical) or 4px tall (horizontal)
       - Background: var(--border-color), on hover: var(--color-accent) with 0.5 opacity
       - Transition on background-color
  </action>
  <verify>
    <automated>cd /workspaces/venture-os/dashboard/frontend && npx tsc --noEmit && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>Three panels render with draggable borders. Left=Chat, Center=Graph (with nested bottom), Right=Workspace. autoSaveId persists sizes to localStorage. Panel refs exposed for keyboard shortcuts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite proxy -> bridge | Dev proxy forwards /api and /ws to bridge server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | localStorage panel sizes | accept | Low risk -- user is sole operator, worst case is reset panel sizes |
| T-02-02 | I (Info Disclosure) | Vite proxy | accept | localhost only, dev environment in Codespace |
</threat_model>

<verification>
1. `cd dashboard/frontend && npm run dev` starts Vite on :5180
2. Opening http://localhost:5180 shows three panels with labels
3. Dragging borders resizes panels
4. Refreshing browser restores panel sizes
</verification>

<success_criteria>
- Three labeled panels visible in browser
- Panel borders are draggable
- Panel sizes persist across refresh (autoSaveId)
- TypeScript compiles clean
- Vite proxies /api to bridge on :3100
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-layout/02-01-SUMMARY.md`
</output>
