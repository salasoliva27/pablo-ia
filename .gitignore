.env
*.credentials.json
node_modules/
.DS_Store
.playwright-mcp/
outputs/screenshots/
*.png
# Obsidian personal state (do not commit — machine-specific)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
# Chat file uploads — user-submitted, not versioned
dump/uploads/
# Local Claude Code state — machine-specific, not versioned
.claude/settings.local.json
.claude/scheduled_tasks.lock
**/.claude/settings.local.json
# Runtime logs
*.log
# Generated binaries — these go to Drive (Janus_AI/), never git.
# See CLAUDE.md STORAGE ROUTING.
outputs/**/*.pdf
outputs/**/*.docx
outputs/**/*.xlsx
outputs/**/*.pptx
outputs/**/*.odt
outputs/**/*.mp4
outputs/**/*.mov
outputs/**/*.webm
outputs/**/*.avi
outputs/**/*.mp3
outputs/**/*.wav
outputs/**/*.zip
outputs/**/*.tar.gz
outputs/**/*.jpg
outputs/**/*.jpeg
outputs/**/*.webp
outputs/**/*.gif
